# Structured match log export

Ballsfoot keeps match logs as structured browser-side telemetry, not only visible timeline copy or console text. This preserves the single-file local workflow while making each match exportable as text, JSONL, CSV, and full JSON with summary and realism signals, so future tuning can be based on observed match patterns instead of vibes.
